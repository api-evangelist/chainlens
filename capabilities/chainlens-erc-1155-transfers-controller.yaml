naftiko: 1.0.0-alpha2
info:
  label: Chainlens — Erc-1155-Transfers-Controller
  description: 'Chainlens — Erc-1155-Transfers-Controller. 2 operations. Lead operation: Chainlens Retrieve transfers of an
    ERC1155 Token.. Self-contained Naftiko capability covering one Chainlens business surface.'
  tags:
  - Chainlens
  - Erc-1155-Transfers-Controller
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CHAINLENS_API_KEY: CHAINLENS_API_KEY
capability:
  consumes:
  - type: http
    namespace: chainlens-erc-1155-transfers-controller
    baseUri: ''
    description: Chainlens — Erc-1155-Transfers-Controller business capability. Self-contained, no shared references.
    resources:
    - name: tokens-addressHash-erc1155-transfers
      path: /tokens/{addressHash}/erc1155/transfers
      operations:
      - name: finderc1155transfers
        method: GET
        description: Chainlens Retrieve transfers of an ERC1155 Token.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: addressHash
          in: path
          type: string
          description: The address hash identifying the token.
          required: true
        - name: query
          in: query
          type: string
          required: true
    - name: tokens-addressHash-erc1155-transfers-tokenId
      path: /tokens/{addressHash}/erc1155/transfers/{tokenId}
      operations:
      - name: finderc1155transfers1
        method: GET
        description: Chainlens Retrieve transfers of an ERC1155 Token.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: addressHash
          in: path
          type: string
          description: The address hash identifying the token.
          required: true
        - name: tokenId
          in: path
          type: string
          description: The address hash identifying the token.
          required: true
        - name: query
          in: query
          type: string
          required: true
  exposes:
  - type: rest
    namespace: chainlens-erc-1155-transfers-controller-rest
    port: 8080
    description: REST adapter for Chainlens — Erc-1155-Transfers-Controller. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/tokens/{addresshash}/erc1155/transfers
      name: tokens-addresshash-erc1155-transfers
      description: REST surface for tokens-addressHash-erc1155-transfers.
      operations:
      - method: GET
        name: finderc1155transfers
        description: Chainlens Retrieve transfers of an ERC1155 Token.
        call: chainlens-erc-1155-transfers-controller.finderc1155transfers
        with:
          addressHash: rest.addressHash
          query: rest.query
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/tokens/{addresshash}/erc1155/transfers/{tokenid}
      name: tokens-addresshash-erc1155-transfers-tokenid
      description: REST surface for tokens-addressHash-erc1155-transfers-tokenId.
      operations:
      - method: GET
        name: finderc1155transfers1
        description: Chainlens Retrieve transfers of an ERC1155 Token.
        call: chainlens-erc-1155-transfers-controller.finderc1155transfers1
        with:
          addressHash: rest.addressHash
          tokenId: rest.tokenId
          query: rest.query
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: chainlens-erc-1155-transfers-controller-mcp
    port: 9090
    transport: http
    description: MCP adapter for Chainlens — Erc-1155-Transfers-Controller. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: chainlens-retrieve-transfers-erc1155-token
      description: Chainlens Retrieve transfers of an ERC1155 Token.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: chainlens-erc-1155-transfers-controller.finderc1155transfers
      with:
        addressHash: tools.addressHash
        query: tools.query
      outputParameters:
      - type: object
        mapping: $.
    - name: chainlens-retrieve-transfers-erc1155-token-2
      description: Chainlens Retrieve transfers of an ERC1155 Token.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: chainlens-erc-1155-transfers-controller.finderc1155transfers1
      with:
        addressHash: tools.addressHash
        tokenId: tools.tokenId
        query: tools.query
      outputParameters:
      - type: object
        mapping: $.
